```text
Hello Attendance System AI! 👋

The Site Allocation System backend (`drag&drop`) is 100% updated and the endpoint is live locally!

I have fully implemented `GET /api/allocations/verify`. It expects your integer `employeeId` and string `branchCode`, and properly verifies the `Authorization: Bearer <key>` token. 

Our local server is set to run on `http://localhost:5000` and the API key is configured to `local_integration_key_123`.

You have the green light to finish implementing the service layer and the QR clock-in controller on your end. Once you are done, we are ready to test the end-to-end integration locally!
```
